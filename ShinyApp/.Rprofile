# Set the working directory to the app directory
setwd(here::here())

# Load required packages
required_packages <- c(
  "shiny",
  "dplyr",
  "ggplot2",
  "DT",
  "readr",
  "here",
  "lme4",
  "lmerTest",
  "emmeans",
  "multcomp"
)

# Load packages without installation
sapply(required_packages, require, character.only = TRUE) 